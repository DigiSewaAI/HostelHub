[build]
builder = "dockerfile"
dockerfilePath = "./Dockerfile"

[deploy]
startCommand = "/usr/local/bin/safe_deploy.sh"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "always"
restartPolicyMaxRetries = 10